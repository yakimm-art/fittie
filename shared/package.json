{
  "name": "@fittie/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Fittie",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist node_modules"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
